## Redis

인메모리 데이터베이스인 Redis를 Python에서 조작할 수 있도록 해주는 패키지이다.

### 패키지 설치

``` bash
pip install redis
```

### 템플릿

``` python
from redis import Redis

redis = Redis('<Redis 주소>', decode_responses=True)

# 데이터 저장(Set)
redis.set('<키>', '<값>')

# 데이터 조회(Get)
data = redis.get('<키>')
```